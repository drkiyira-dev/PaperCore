# PaperCore 环境变量模板
# 用法：复制为 .env 并按需填写。.env 已在 .gitignore 中，不会被提交。
# 所有变量都是可选的——不填也能跑（走本地规则 / 本地模型路径）。

# 云端 DeepSeek（可选）：填了才会用云端大模型做深度分析。
# 不填则首页可改用「本地大模型（Ollama）」或仅本地规则分析。
DEEPSEEK_API_KEY=
# DeepSeek 模型名（可选，默认 deepseek-chat）
# DEEPSEEK_MODEL=deepseek-chat

# 本地大模型 Ollama（可选，均有默认值，一般无需改）
# OLLAMA_HOST=http://127.0.0.1:11434
# OLLAMA_MODEL=deepseek-r1:7b
